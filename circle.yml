machine:
  node:
    version: 8

deployment:
  prod:
    branch: master
    commands:
      # not every commit is going to be published
      - npm_config_loglevel=verbose npm run semantic-release || true
